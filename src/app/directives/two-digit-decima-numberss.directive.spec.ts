import { TwoDigitDecimaNumberssDirective } from './two-digit-decima-numberss.directive';

describe('TwoDigitDecimaNumberssDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoDigitDecimaNumberssDirective();
    expect(directive).toBeTruthy();
  });
});
